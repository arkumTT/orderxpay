# --- env / secrets ---
.env
.env.local
.env.*.local
*.pem
*.key
src/api/firebase-service-account.json
# Real per-project Firebase config (API key, project id) for the Android
# app's push-notification setup (Section 4.10 Phase 2) — not committed for
# the same reason .env isn't; place it manually after cloning (Firebase
# console > Project settings > General > download google-services.json).
src/mobile/android/app/google-services.json

# --- OS ---
.DS_Store
Thumbs.db

# --- Go (src/api) ---
src/api/bin/
src/api/tmp/
src/api/uploads/
src/api/private_kyc_uploads/
*.exe
*.test
*.out

# --- Node / Next.js (src/back-office, src/web) ---
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*

# --- Flutter (src/mobile) ---
src/mobile/.dart_tool/
src/mobile/.flutter-plugins
src/mobile/.flutter-plugins-dependencies
src/mobile/.pub-cache/
src/mobile/.pub/
src/mobile/build/
src/mobile/ios/Pods/
src/mobile/ios/.symlinks/
src/mobile/android/.gradle/
src/mobile/**/*.g.dart

# --- editors ---
.vscode/
.idea/

# --- local tool config ---
.claude/settings.local.json
