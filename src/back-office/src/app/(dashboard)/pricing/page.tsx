import { PlaceholderPage } from "@/components/placeholder-page";

export default function PricingPage() {
  return (
    <PlaceholderPage
      title="Pricing, Commission & Feature Configuration"
      section="7.4"
      description="Global default commission rate with merchant-specific overrides, default service-charge allocation, and feature flags for rolling out new modules."
    />
  );
}
