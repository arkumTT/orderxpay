import { PlaceholderPage } from "@/components/placeholder-page";

export default function IntegrationsPage() {
  return (
    <PlaceholderPage
      title="System Integrations Hub"
      section="7.3"
      description="Configuration for PSP, WhatsApp BSP, SMS/Email provider, USSD aggregator, and the optional GRA E-VAT connector, plus the maintained list of verified delivery providers."
    />
  );
}
