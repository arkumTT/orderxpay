import { PlaceholderPage } from "@/components/placeholder-page";

export default function AdminUsersPage() {
  return (
    <PlaceholderPage
      title="Admin Users, Roles & Permissions"
      section="7.8"
      description="Role-based access for the back-office team: Super Admin, Compliance/KYC Reviewer, Finance/Settlement, Support — each scoped to only the data and actions they need."
    />
  );
}
