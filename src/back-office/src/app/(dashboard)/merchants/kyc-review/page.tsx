import { PlaceholderPage } from "@/components/placeholder-page";

export default function KycReviewPage() {
  return (
    <PlaceholderPage
      title="KYC Review Queue"
      section="7.1"
      description="View submitted Ghana Card / business registration documents, approve or reject with a reason, request additional documents."
    />
  );
}
