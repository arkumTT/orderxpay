import { PlaceholderPage } from "@/components/placeholder-page";

export default function MerchantsPage() {
  return (
    <PlaceholderPage
      title="Merchants"
      section="7.1"
      description="Merchant list with KYC tier, status, GMV, and join date. Merchant detail view: profile, catalog snapshot, transaction history, payout history, notes/flags."
    />
  );
}
