import { PlaceholderPage } from "@/components/placeholder-page";

export default function SupportPage() {
  return (
    <PlaceholderPage
      title="Support / Helpdesk Console"
      section="7.10"
      description="Look up a merchant's or customer's transaction, resend a payment link, or escalate a payout issue, without needing raw database access."
    />
  );
}
